/**
 * Acentos de la tienda (neutro, alineado al logo).
 * Expuestos en `body` como `--store-accent` y `--store-accent-hover`.
 */
export const STORE_ACCENT = "#1c1917" as const;
export const STORE_ACCENT_HOVER = "#292524" as const;
