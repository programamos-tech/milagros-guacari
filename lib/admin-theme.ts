/**
 * Fondo del sidebar del panel (`--admin-sidebar-bg` en `app/layout.tsx`).
 * Rosa suave alineado a la marca de vitrina.
 */
export const ADMIN_SIDEBAR_BG = "#ffb8d4" as const;

/** Paneles suaves (cuenta, direcciones) — blanco rosado muy suave, alineado a la vitrina. */
export const STORE_CHROME_BG = "#fff9fb" as const;
